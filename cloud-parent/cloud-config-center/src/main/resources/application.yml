server:
   port: 3344
debug: true

spring:
   application:
      name: cloud-config-center
   cloud:
      config:
         server:
            git:
               uri: https://github.com/115057433/cloud-config-center